import { assert } from 'chai'

import { mappingReplacer } from '../src/replacers'
import { CHAR_MAP } from '../src/encodingsMap'

describe('replacers', () => {
    describe('replaceStrings', () => {
        it('returns something', () => {
            assert.equal(true,true)
        })  
    })
})